<% if (title) { %>
  <h1><%= title %></h1>
<% } else { %>
  <h1>Inventory Management</h1>
<% } %>

<% if (message && message.notice) { %>
  <% (Array.isArray(message.notice) ? message.notice : [message.notice]).forEach(msg => { %>
    <p class="notice"><%= msg %></p>
  <% }) %>
<% } %>

<nav class="management-links">
  <ul>
    <li><a href="/inv/add-classification">Add New Classification</a></li>
    <li><a href="/inv/add-inventory">Add New Inventory Item</a></li>
  </ul>
</nav>

<h2>Manage Inventory</h2>
<p>Select a classification from the list below to see the items belonging to that classification:</p>
<%- classificationList %>
<table id="inventoryDisplay"></table>
<noscript>JavaScript must be enabled to use this page.</noscript>

<script src="../../js/inventory.js"></script>